1. OptaPlanner
简介：OptaPlanner 是一个基于 Java 的开源约束优化求解器，专注于排班、车辆路径、装箱、时间表等组合优化问题。
适用问题：排班、排课、车辆路径、装箱、作业车间调度等 NP-hard 问题。
建模方式：面向对象，使用 Java 类描述实体、变量和约束，约束用 Java 代码或 Drools 规则表达。
求解算法：主要采用元启发式算法（如局部搜索、模拟退火、禁忌搜索、遗传算法等），适合大规模、复杂约束的实际问题。
优点：
适合复杂、实际的调度和分配问题
易于与 Java 企业应用集成
社区活跃，文档丰富
缺点：
不适合纯数学规划（线性/整数规划）问题
主要面向 Java 生态

2. OR-Tools
简介：OR-Tools 是 Google 开源的优化工具包，支持多种优化问题（线性规划、整数规划、约束规划、车辆路径、作业调度等）。
适用问题：线性/整数规划、约束满足、车辆路径、作业调度、TSP、SAT 等。
建模方式：支持 Python、C++、Java、C# 等多语言 API，建模灵活，约束表达能力强。
求解算法：集成多种求解器（CP-SAT、SCIP、GLOP、CBC 等），自动选择最优算法。
优点：
多语言支持，易于集成
适合多种优化问题，尤其是调度、路径规划
性能优良，Google 级别工程质量
缺点：
某些高级功能文档较少
建模风格与传统数学规划工具略有不同

3. Pyomo
简介：Pyomo 是 Python 生态下的通用数学建模语言，支持线性规划（LP）、整数规划（MIP）、非线性规划（NLP）、约束规划等。
适用问题：LP、MIP、NLP、约束优化等数学规划问题。
建模方式：用 Python 代码描述变量、目标、约束，语法接近数学表达式，支持数据驱动建模。
求解算法：本身不带求解器，可调用 CBC、GLPK、HiGHS、SCIP、CPLEX、Gurobi 等多种求解器。
优点：
语法灵活，适合复杂数学模型
支持多种求解器，易于切换
适合科研、教学和工业应用
缺点：
主要面向数学规划，不适合元启发式/大规模调度
对于极大规模问题，性能取决于所用求解器

4. JobShopScheduler
简介：JobShopScheduler 通常指一类专门用于作业车间调度（Job Shop Scheduling）问题的工具或库。没有一个统一的“JobShopScheduler”软件，但有多个实现（如 Python 的 jobshop-scheduler、OR-Tools 的 Job Shop 模块等）。
适用问题：作业车间调度（JSP）、流水线调度、工厂生产排程等。
建模方式：通常提供专门的 API 或数据结构描述工件、工序、机器、加工时间等。
求解算法：可能采用精确算法（如整数规划）、约束规划或元启发式（如遗传算法、局部搜索）。
优点：
针对 JSP 问题优化，建模简便
适合生产制造、工厂调度等实际场景
缺点：
适用范围窄，主要针对 JSP
灵活性和通用性不如 Pyomo、OR-Tools

工具	            主要语言	      适用问题类型	      建模方式	        求解算法	        适合场景
OptaPlanner	      Java	      排班、调度、路径等	  面向对象/规则	  元启发式	        复杂调度、排班
OR-Tools	        多语言      	LP/MIP/CP/调度等	    API/约束表达	    多种（CP、MIP等）路径、调度、规划
Pyomo	            Python	    LP/MIP/NLP/CP等	    类数学表达	      调用外部求解器	  数学规划、科研
JobShopScheduler	多语言	      作业车间调度	        专用API/数据结构	多种	          工厂生产调度



example I： 
dexpr int absenceOfTaskUrgentRQT = 
    sum(t in Tasks: t.seq != 0 
        && RemainingQTime1[t.foup].rqtseq == t.seq 
        && RemainingQTime1[t.foup].rqt > 0 
        && RemainingQTime1[t.foup].rqt <= 60 
        && IsActive[t.foup] == 1 
        && matchAt(RemainingQTime1[t.foup].rqteqptype, "CAROZ") == -1
    ) ((!presenceOf(tasks[t])))
  + sum(t in Tasks: t.seq != 0 
        && RemainingQTime1[t.foup].rqtseq == t.seq 
        && RemainingQTime1[t.foup].rqt <= 0
    ) ((!presenceOf(tasks[t])) * 100);

1. dexpr int absenceOfTaskUrgentRQT = ...
dexpr：声明一个派生表达式变量（derived expression），类型为 int。
absenceOfTaskUrgentRQT：变量名，表示“紧急Q-time任务未被调度的计数/惩罚”。

2. 第一部分 sum(...) ((!presenceOf(tasks[t])))
遍历所有任务 t，筛选条件如下：

t.seq != 0：任务序号不为0（有效任务）。
RemainingQTime1[t.foup].rqtseq == t.seq：该任务正好是Foup当前Q-time监控的工序。
RemainingQTime1[t.foup].rqt > 0 && RemainingQTime1[t.foup].rqt <= 60：Q-time剩余时间在(0, 60]分钟内，属于紧急Q-time。
IsActive[t.foup] == 1：Foup处于激活状态。
matchAt(RemainingQTime1[t.foup].rqteqptype, "CAROZ") == -1：Q-time对应的设备类型不是"CAROZ"（即排除某些特殊设备类型）。
统计内容：

!presenceOf(tasks[t])：如果任务 t 没有被调度（interval variable 不存在），则为1，否则为0。
也就是说，每有一个紧急Q-time任务未被调度，计数加1。

3. 第二部分 sum(...) ((!presenceOf(tasks[t])) * 100)
遍历所有任务 t，筛选条件如下：

t.seq != 0
RemainingQTime1[t.foup].rqtseq == t.seq
RemainingQTime1[t.foup].rqt <= 0：Q-time已经超时（剩余时间≤0）。
统计内容：

!presenceOf(tasks[t]) * 100：如果该任务未被调度，计数加100（惩罚更重）。

4. 整体含义
该表达式统计了两类未被调度的任务数量：
紧急Q-time任务（剩余Q-time在0~60分钟内，且不是特殊设备类型）：每漏掉一个，计1分。
Q-time已超时的任务：每漏掉一个，计100分（惩罚极大，强烈避免）。
该变量通常会被用在目标函数或约束中，鼓励模型优先调度紧急Q-time任务，极力避免Q-time超时任务未被调度。

5.总结
统计所有“快要Q-time超时”的任务，如果没被调度，计1分；
统计所有“已经Q-time超时”的任务，如果没被调度，计100分；
这个变量越小越好，模型会优先安排这些任务，避免Q-time违例。

example II: 计算紧急Q-time任务的Q-time违例量
dexpr int rqtViolationUrgent = 
      sum(t in Tasks: t.seq != 0 && RemainingQTime1[t.foup].rqtseq == t.seq 
	&& RemainingQTime1[t.foup].rqt > 0 && RemainingQTime1[t.foup].rqt <= 60 && IsActive[t.foup] == 1 && matchAt(RemainingQTime1[t.foup].rqteqptype, "CAROZ") == -1) 
	maxl(0, (startOf(tasks[t]) - RemainingQTime1[t.foup].rqt + QtimeUrgent))
	+ sum(t in Tasks: t.seq != 0 && RemainingQTime1[t.foup].rqtseq == t.seq 
	&& RemainingQTime1[t.foup].rqt <= 0) ((startOf(tasks[t]) - RemainingQTime1[t.foup].rqt) * 100);

rqtViolationUrgent 统计了所有紧急Q-time任务和已超时任务的Q-time违例量。
对于紧急Q-time任务，只有超时部分才计入违例（未超时为0）。
对于已超时任务，违例量乘以100，极大加重惩罚。尽量让Over Qtime的任务先排
该变量通常用于目标函数，鼓励模型优先调度紧急Q-time任务，极力避免Q-time超时。

