using CP;

int NbFoups;
tuple FoupInfo {
   key string foup;
   float priority;
   int waitingtime;
   int isactive;
   int availfordispatch;
   int ignoreBatchSize; //
   string rqteqptype1;
   int rqtseq1;
   int rqt1;
   string rqteqptype2;
   int rqtseq2;
   int rqt2;
   string rqteqptype3;
   int rqtseq3;
   int rqt3;
};
{FoupInfo} FoupInfos;  

int NbEqps;
tuple Eqp {
	key string eqp;
	string eqptype;
	int availtime;
	int stopdispatchtime;
    int rtdeqptype;
};
{Eqp} Eqps;  

int NbChambers;
tuple Chamber {
    key string chamber;
    string type;
	string eqp;
	string eqptype;
	int availtime;
	int stopdispatchtime;
	int is40KD22;
};
{Chamber} Chambers;  

int NbTasks;
tuple Task {
	string foup;
	int seq;
	string recipe;
	int component;
    string eqptype;
    int isprocess;
    int assignedpriority;
    string qtimeeqptype1;
    int qtimeseq1;
    int qtimelimit1;
    string qtimeeqptype2;
    int qtimeseq2;
    int qtimelimit2;
    string qtimeeqptype3;
    int qtimeseq3;
    int qtimelimit3;
};
{Task} Tasks;

int NbPPIDs;
tuple PPID {
    string ppid;
    int ppidnumber;
	string foup;
	int seq;
	string recipe;
	string eqp;
	string eqptype;
	int ptime;
	int advancedinhibit;
	int minbatchwafers; //
	int maxbatchwafers; //
	int batchsize; //
	int minwafercountperbatch; //
	int eqrecoverystart;
	int eqrecoveryend;
	{string} chamber;
};
{PPID} PPIDs;
{string} chambersrequired [1..NbPPIDs];

int TransitTime;
int DueDate;
int QtimeUrgent;
int QtimeBuffer;

int NbCUSOVTStopPeriod;
tuple CUSOVTStopPeriod
{
    string eqp;
    int CUSOVTNextCCStopTime;
    int CUSOVTNextCCStartTime;
};
{CUSOVTStopPeriod} CUSOVTStopPeriods;

{string} Foups;
float Priority[f in Foups];
int WaitingTime[f in Foups];
int IsActive[f in Foups]; 
int AvailForDispFoup[f in Foups]; 
int IgnoreBatchSize[f in Foups]; 

tuple RQT {
   string rqteqptype;
   int rqtseq;
   int rqt;
};
RQT RemainingQTime1[f in Foups]; 
RQT RemainingQTime2[f in Foups]; 
RQT RemainingQTime3[f in Foups]; 

{Chamber} AllChambers;
{string} AllChamberIds;
{Task} CUSOVTTasks; 
{PPID} CUSOVTPPIDs;
{Eqp} CUSOVTEqps; 
{Eqp} ToolEqps;  
{Eqp} BatchEqps;
//{Chamber} CUOXEChs;  

tuple Assignment {
   Task task;
   PPID ppid;
   Eqp eqp; 
};
{Assignment} Assignments; 
{Assignment} Assignments_Supp; 

int MinDispatchTime[a in Assignments]; 
int StopDispatchTime[a in Assignments]; 
int CuoxeLotSum;
int CuoxeLotis40KD22;



string strTemp1;
string strTemp2;
string strTemp3;
string strTemp4;
string strTemp5;
string strTemp6;
string strTemp7;
string strTemp8;
string strTemp9;
string strTemp10;
string strTemp11;
string strTemp12;
string strTemp13;
string strTemp14;
string strTemp15;
string strTemp16;
string strTemp17;
string strTemp18;
string strTemp19;
string strTemp20;
string strTemp21;
string strTemp22;
string strTemp23;

execute
{
    var dataFile = new IloOplInputFile("data.txt");
    var str = "";
    NbFoups = dataFile.readline();
    for (var i = 1; i <= NbFoups; i++) {
      str = dataFile.readline();  
      strTemp1 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp2 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp3 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp4 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp5 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp6 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp7 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp8 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp9 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp10 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp11 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp12 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp13 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp14 = str.substring(0, str.indexOf(" "));
      str = str.substring(str.indexOf(" ") + 1);
      strTemp15 = str;
      FoupInfos.add(strTemp1, strTemp2, strTemp3, strTemp4, strTemp5, strTemp6, strTemp7, strTemp8, strTemp9, strTemp10, strTemp11, strTemp12, strTemp13, strTemp14, strTemp15); 
	}     
    NbEqps = dataFile.readline();
    for (var i = 1; i <= NbEqps; i++) {
    	str = dataFile.readline();  
    	strTemp1 = str.substring(0, str.indexOf(" "));
    	str = str.substring(str.indexOf(" ") + 1);
     	strTemp2 = str.substring(0, str.indexOf(" "));
    	str = str.substring(str.indexOf(" ") + 1);
      	strTemp3 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp4 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp5 = str;
      	Eqps.add(strTemp1, strTemp2, strTemp3, strTemp4, strTemp5);  
    }   
    
    NbCUSOVTStopPeriod = dataFile.readline();
    for (var i = 1; i <= NbCUSOVTStopPeriod; i++) {
    	str = dataFile.readline();  
    	strTemp1 = str.substring(0, str.indexOf(" "));
    	str = str.substring(str.indexOf(" ") + 1);
     	strTemp2 = str.substring(0, str.indexOf(" "));
    	str = str.substring(str.indexOf(" ") + 1);
      	strTemp3 = str;
      	CUSOVTStopPeriods.add(strTemp1, strTemp2, strTemp3);    
    }
     
    NbChambers = dataFile.readline();
    for (var i = 1; i <= NbChambers; i++) {
    	str = dataFile.readline();  
    	strTemp1 = str.substring(0, str.indexOf(" "));
    	str = str.substring(str.indexOf(" ") + 1);
     	strTemp2 = str.substring(0, str.indexOf(" "));
    	str = str.substring(str.indexOf(" ") + 1);
      	strTemp3 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp4 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp5 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
		strTemp6 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp7 = str;
      	Chambers.add(strTemp1, strTemp2, strTemp3, strTemp4, strTemp5, strTemp6, strTemp7);  
      	if (strTemp4 == "CUOXE" || strTemp4 == "CUSTRIP" || strTemp4 == "CUSOVT") // || strTemp4 == "CUSINE" || strTemp4 == "CUMETE" || strTemp4 == "CUSOVT" 
      	  AllChamberIds.add(strTemp1);
    }    
    NbTasks = dataFile.readline();
    for (var i = 1; i <= NbTasks; i++) {
      	str = dataFile.readline();  
      	strTemp1 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp2 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp3 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp4 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp5 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp6 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp7 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp8 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp9 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp10 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp11 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp12 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp13 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp14 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp15 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp16 = str;
      	Tasks.add(strTemp1, strTemp2, strTemp3, strTemp4, strTemp5, strTemp6, strTemp7, strTemp8, strTemp9, strTemp10,
      	strTemp11, strTemp12, strTemp13, strTemp14, strTemp15, strTemp16);  
  	}   
    NbPPIDs = dataFile.readline(); 
    for (var i = 1; i <= NbPPIDs; i++) {
      	str = dataFile.readline();  
      	strTemp1 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp2 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp3 = str.substring(0, str.indexOf(" "));
     	str = str.substring(str.indexOf(" ") + 1);
      	strTemp4 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp5 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp6 = str.substring(0, str.indexOf(" "));
        str = str.substring(str.indexOf(" ") + 1);
      	strTemp7 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp8 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp9 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp10 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp11 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp12 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp13 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp14 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp15 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp16 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp17 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp18 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp19 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp20 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp21 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp22 = str.substring(0, str.indexOf(" "));
      	str = str.substring(str.indexOf(" ") + 1);
      	strTemp23 = str;
      	if (strTemp16 != "0")	
      		chambersrequired[i].add(strTemp16);
      	if (strTemp17 != "0")	
      		chambersrequired[i].add(strTemp17);
      	if (strTemp18 != "0")	
      		chambersrequired[i].add(strTemp18);	
      	if (strTemp19 != "0")
      		chambersrequired[i].add(strTemp19);	
      	if (strTemp20 != "0")	
      		chambersrequired[i].add(strTemp20);	
      	if (strTemp21 != "0")	
      		chambersrequired[i].add(strTemp21);	
      	if (strTemp22 != "0")	
      		chambersrequired[i].add(strTemp22);	
      	if (strTemp23 != "0")	
      		chambersrequired[i].add(strTemp23);	
		PPIDs.add(strTemp1, strTemp2, strTemp3, strTemp4, strTemp5, strTemp6, strTemp7, strTemp8, strTemp9, strTemp10, strTemp11, strTemp12, strTemp13, strTemp14, strTemp15, chambersrequired[i]);  
	} 
	
    //PPIDNumber = dataFile.readline();
    TransitTime = dataFile.readline();  
    DueDate = dataFile.readline();  
    QtimeUrgent = dataFile.readline();  
    QtimeBuffer = dataFile.readline(); 
	CuoxeLotSum = dataFile.readline();
	CuoxeLotis40KD22 = dataFile.readline();
    dataFile.close();
    
    for (var f in FoupInfos) 
      Foups.add(f.foup);  
 
    for (var f in FoupInfos) {  
      Priority[f.foup] = f.priority;
      WaitingTime[f.foup] = f.waitingtime;
      IsActive[f.foup] = f.isactive;
      AvailForDispFoup[f.foup] = f.availfordispatch; 
      RemainingQTime1[f.foup].rqteqptype = f.rqteqptype1;
      RemainingQTime1[f.foup].rqtseq = f.rqtseq1;
      RemainingQTime1[f.foup].rqt = f.rqt1;
      RemainingQTime2[f.foup].rqteqptype = f.rqteqptype2;
      RemainingQTime2[f.foup].rqtseq = f.rqtseq2;
      RemainingQTime2[f.foup].rqt = f.rqt2;
      RemainingQTime3[f.foup].rqteqptype = f.rqteqptype3;
      RemainingQTime3[f.foup].rqtseq = f.rqtseq3;
      RemainingQTime3[f.foup].rqt = f.rqt3;
    } 
    
    for (var ch in Chambers) 
  	  if (AllChamberIds.contains(ch.chamber)) 
  	    AllChambers.add(ch);        
  	   
  	for (var t in Tasks)
  	  if (t.eqptype == "CUSOVT") 
  	    CUSOVTTasks.add(t);
    
    for (var p in PPIDs)
  	  if (p.eqptype == "CUSOVT")
  	    CUSOVTPPIDs.add(p);
      	
  	for (var e in Eqps)
  	  if (e.eqptype == "CUSOVT")
  	    CUSOVTEqps.add(e);  
  	
    for (var e in Eqps)
  	  if (e.eqptype == "CUSPUT" || e.eqptype == "CUSILPE" || e.eqptype == "CUPLATE") // || e.eqptype == "CUALLOY"
  	    ToolEqps.add(e);  
  	    	         
 	for (var e in Eqps)
  	  if (e.rtdeqptype == 1)
  	    BatchEqps.add(e);  
		
	for (var t in Tasks)    
      for (var p in PPIDs) 
        for (var e in Eqps)
            if (p.eqp == e.eqp && p.foup == t.foup && p.seq == t.seq && p.recipe == t.recipe && p.eqptype == t.eqptype) 
              Assignments.add(t, p, e);  
    
    for (var t in CUSOVTTasks)     
      for (var p in CUSOVTPPIDs) 
        for (var e in CUSOVTEqps) 
            if (p.eqp == e.eqp && p.foup == t.foup && p.seq == t.seq && p.recipe == t.recipe && p.eqptype == t.eqptype)
              Assignments_Supp.add(t, p, e);     
    
         
    for (var a in Assignments)
      if(a.eqp.eqptype == "CUOXE" || a.eqp.eqptype == "CUSTRIP" || a.eqp.eqptype == "CUSOVT") // || a.eqp.eqptype.contains("CUSINE")
        for(var ch in Chambers)
          if(a.ppid.chamber.contains(ch.chamber))
            MinDispatchTime[a] = Opl.maxl(ch.availtime, AvailForDispFoup[a.task.foup]); 
    
	for (var a in Assignments)
      if(a.eqp.eqptype != "CUOXE" && a.eqp.eqptype != "CUSTRIP" && a.eqp.eqptype != "CUSOVT")
        MinDispatchTime[a] = Opl.maxl(a.eqp.availtime, AvailForDispFoup[a.task.foup]);  
   
    for (var a in Assignments)
      StopDispatchTime[a] = DueDate;  
    for (var a in Assignments) 
      for (var ch in Chambers) 
        if (a.ppid.chamber.contains(ch.chamber)) 
          StopDispatchTime[a] = Opl.minl(ch.stopdispatchtime, StopDispatchTime[a]);  
    for (var a in Assignments) 
      if (a.eqp.stopdispatchtime < DueDate) 
        StopDispatchTime[a] = Opl.minl(a.eqp.stopdispatchtime, StopDispatchTime[a]);                

    for (var f in Foups) 
      if ((RemainingQTime1[f].rqt <= QtimeUrgent && RemainingQTime1[f].rqteqptype.indexOf("CUOXE") == -1) || 
      	  (RemainingQTime2[f].rqt <= QtimeUrgent && RemainingQTime2[f].rqteqptype.indexOf("CUOXE") == -1) || 
      	  (RemainingQTime3[f].rqt <= QtimeUrgent && RemainingQTime3[f].rqteqptype.indexOf("CUOXE") == -1)) 	
	        IgnoreBatchSize[f] = 1;   			
}
